name: QA

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  check-whitespace:
    uses: ./.github/workflows/check-whitespace.yml
  build-and-test:
    uses: ./.github/workflows/build.yml
    needs: check-whitespace
